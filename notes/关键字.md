# V语言目前共有以下21个关键词
    break
    const
    continue
    defer
    else
    enum
    fn
    for
    go
    goto
    if
    import
    in
    interface
    match
    module
    mut
    or
    return
    struct
    type